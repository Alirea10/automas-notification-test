[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "automas_notification_test"
version = "0.0.2"
description = "Send a notification test message through all registered notify channels on startup"
readme = { file = "README.md", content-type = "text/markdown" }
requires-python = ">=3.10"
dependencies = ["pydantic>=2.0"]

[project.entry-points."auto_mas.plugins"]
notification_test = "notification_test.plugin:Plugin"

[tool.setuptools.packages.find]
where = ["src"]
